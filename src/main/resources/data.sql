CREATE TABLE user(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    fullname VARCHAR(100) NOT NULL,
    username VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE
)

CREATE TABLE word_q(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    en VARCHAR(50),
    tr VARCHAR(50),
    img VARCHAR(100),
    def VARCHAR(250)
)
CREATE TABLE word_a(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    en VARCHAR(50),
    tr VARCHAR(50),
    img VARCHAR(100),
    def VARCHAR(250)
)
CREATE TABLE word_qa(
)
CREATE TABLE sentence_q(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    en VARCHAR(250),
    tr VARCHAR(250),
    img VARCHAR(100),
    quotes VARCHAR(50)
)
CREATE TABLE sentence_a(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    en VARCHAR(250),
    tr VARCHAR(250),
    img VARCHAR(100),
    quotes VARCHAR(50)
)
CREATE TABLE sentence_qa(
)
CREATE TABLE comment(
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    comment VARCHAR(250),
    sentence_id INT,
    word_id INT,
)